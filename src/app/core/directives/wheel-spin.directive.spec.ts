import { WheelSpinDirective } from './wheel-spin.directive';

describe('WheelSpinDirective', () => {
  it('should create an instance', () => {
    const directive = new WheelSpinDirective();
    expect(directive).toBeTruthy();
  });
});
